# DO NOT EDIT. Generated with:
#
#    devctl
#
#    https://github.com/giantswarm/devctl/blob/57c68171868158d915a92dd45201bbeadaa17214/pkg/gen/input/workflows/internal/file/dispatch_update_chart_events.yaml.template
#
name: Dispatch update-chart events to security-bundle

on:
  push:
    branches:
      - 'main#update-chart'
  pull_request:
    types: [closed]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions: {}

jobs:
  dispatch-events:
    if: github.event_name == 'push'
    uses: giantswarm/github-workflows/.github/workflows/dispatch-update-chart-events.yaml@main
    permissions:
      actions: read
      statuses: read
    with:
      target_repository: 'security-bundle'
    secrets: inherit

  dispatch-pr-closed-events:
    if: github.event_name == 'pull_request' && github.head_ref == 'main#update-chart'
    uses: giantswarm/github-workflows/.github/workflows/dispatch-update-chart-pr-closed-events.yaml@main
    with:
      target_repository: 'security-bundle'
    secrets: inherit
